![labs3card.png](../assets/labs3card.png)
> Лабораторна #3.5
[pdf](./Лабораторна_робота_3.5.pdf)

## 📝 Задача
 - Спроєктувати та розробити функціонал згідно з варіантом (PL, BLL, DAL).
 - Написати модульні тести (Unit Tests) для бізнес-логіки.
 - Використати Mock-об'єкти та Dependency Injection для ізоляції шарів при тестуванні.
 - Реалізувати підрахунок студентів 5-го курсу, що служили в армії.

## 🛠️ Структура проєкту

- `domain`: Сутності `Student`, `FootballPlayer`, `Lawyer`.
- `data-access`: Реалізація `FileRepository` та інтерфейс `IRepository`.
- `business-logic`: Сервіс `StudentService` з бізнес-логікою.
- `app`: Консольний інтерфейс.
- `tests`: Модуль з Unit-тестами (JUnit 5 + MockK).

## 🚀 Як запустити

> [!IMPORTANT]
> Для коректної роботи проєкту необхідно, щоб у системі був встановлений **JDK 17**.

### Запуск додатку
```bash
# Для Windows
gradlew.bat :lab5:run -q --console=plain

# Для Linux/macOS
./gradlew :lab5:run -q --console=plain
```

### Запуск тестів
```bash
# Для Windows
gradlew.bat :lab5:tests:test

# Для Linux/macOS
./gradlew :lab5:tests:test
```

## 🥹 Чек-лист технічних вимог ("Відмінно")

- [x] Виділення рівнів PL, BLL, DAL.
- [x] Покриття бізнес-логіки модульними тестами (JUnit 5).
- [x] Використання принципу Triple A (Arrange, Act, Assert) у тестах.
- [x] Реалізація зв'язку між рівнями за допомогою Dependency Injection (через конструктор).
- [x] Використання Mock-об'єктів (MockK) для імітації DAL у тестах BLL.
- [x] Реалізація всіх завдань варіанту (сутність `Student`, додаткові сутності, специфічні операції).
