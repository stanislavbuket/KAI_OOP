![labs3card.png](../assets/labs3card.png)
> Лабораторна #3.3
[pdf](./Лабораторна_робота_3.3.pdf)"

## 📝 Задача
 - Дослідити механізми серіалізації в Java (Binary, JSON, XML, Custom).
 - Реалізувати типову трирівневу архітектуру (DAL, BLL, PL).
 - Розробити модульну систему для роботи з сутностями (Коло, Студент).
 - Реалізувати вибір стратегії збереження даних під час виконання.

## 🛠️ Структура проєкту
Проєкт розділено на незалежні модулі:

- `domain`: Містить класи сутностей (`Circle`, `Person`, `Student`...) та інтерфейси.
- `data-access`: Відповідає за збереження даних (`DataProvider`, `EntityContext`).
- `business-logic`: Містить бізнес-правила та сервіси (`EntityService`).
- `app`: Точка входу та взаємодія з користувачем (`ConsoleUI`).

## 🚀 Як запустити

> [!IMPORTANT]
> Для коректної роботи проєкту необхідно, щоб у системі був встановлений **JDK 11** або вище.

1.  Перейдіть у кореневу директорію проєкту `KAI_OOP`.
2.  Виконайте наступну команду у вашому терміналі:

    ```bash
    # Для Windows
    gradlew.bat :lab3:run -q --console=plain

    # Для Linux/macOS
    ./gradlew :lab3:run -q --console=plain
    ```

## 🥹 Чек-лист вимог для "Відмінно"

- [x] Розділення рішення на рівні (DAL, BLL, PL) в окремих модулях.
- [x] Реалізація `EntityContext` та `DataProvider` на рівні DAL.
- [x] Реалізація `EntityService` з обробкою винятків `ServiceException` на рівні BLL.
- [x] Консольний інтерфейс `ConsoleUI` в модулі `app`.
- [x] Підтримка бінарної, JSON, XML та користувацької серіалізації.
- [x] Динамічний вибір типу серіалізації та файлу користувачем.
- [x] Використання абстракцій та уникнення повторення коду.
