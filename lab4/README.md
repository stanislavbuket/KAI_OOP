![labs3card.png](../assets/labs3card.png)
> Лабораторна #3.4
[pdf](./Лабораторна_робота_3.4.pdf)

## 📝 Задача
 - Реалізація механізму обробки подій (Event Handling).
 - Створення та використання делегатів (функціональних інтерфейсів) та лямбда-виразів.
 - Сортування масиву символів за спаданням через лямбда-вираз.
 - Реалізація черги з обмеженою ємністю та подією переповнення.

## 🛠️ Структура проєкту

- `domain`: Модуль із класами аргументів подій (`QueueOverflowEventArgs`).
- `business-logic`: Модуль з основною логікою (`CustomQueue`, `CharArraySorter`).
- `app`: Головний модуль із UI (`ConsoleUI`) та точкою входу.

## 🚀 Як запустити

> [!IMPORTANT]
> Для коректної роботи проєкту необхідно, щоб у системі був встановлений **JDK 17**.

1.  Перейдіть у кореневу директорію проєкту `KAI_OOP`.
2.  Виконайте наступну команду у вашому терміналі:

    ```bash
    # Для Windows
    gradlew.bat :lab4:run -q --console=plain

    # Для Linux/macOS
    ./gradlew :lab4:run -q --console=plain
    ```

## 🥹 Чек-лист вимог для "Відмінно"

- [x] Реалізація класу-аргументу подій (`QueueOverflowEventArgs`).
- [x] Клас, у якому генерується подія (`CustomQueue` з методом `enqueue`).
- [x] Клас, у якому є метод-обробник події (`ConsoleUI`).
- [x] Метод-обробник має сигнатуру, подібну до стандартних подій .NET (sender, args).
- [x] Використання лямбда-виразів та анонімних функцій для завдань варіанту.
- [x] Розділення на модулі (Domain, Business Logic, App).
