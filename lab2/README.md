![labs3card.png](../assets/labs3card.png)
> Лабораторна #3.2
[pdf](./Лабораторна_робота_3.2.pdf)

## 📝 Задача
 - Дослідження колекцій та узагальнених колекцій.
 - Реалізація класу **Circle** (Коло) згідно з варіантом 4.
 - Робота зі списками (`List`) та масивами (`Array`).
 - Реалізація власної узагальненої структури даних **Binary Tree** (Бінарне дерево).
 - Реалізація ітератора (`Iterator`) для обходу дерева та підтримка сортування (`Comparable`).

## 🛠️ Структура проєкту

- `app`: Основний модуль, що містить:
    - `Circle.kt`: Клас сутності варіанту.
    - `BinaryTree.kt`: Узагальнена структура даних.
    - `Main.kt`: Демонстрація роботи колекцій та дерева.

## 🚀 Як запустити

> [!IMPORTANT]
> Для коректної роботи проєкту необхідно, щоб у системі був встановлений **JDK 11**.

1.  Перейдіть у кореневу директорію проєкту `KAI_OOP`.
2.  Виконайте наступну команду у вашому терміналі:

    ```bash
    # Для Windows
    gradlew.bat :lab2:run

    # Для Linux/macOS
    ./gradlew :lab2:run
    ```

## 🥹 Чек-лист вимог

- [x] **Задовільно:** Описати клас `Circle` та продемонструвати роботу з колекціями (додавання, видалення, пошук).
- [x] **Добре:** Створити узагальнене бінарне дерево `BinaryTree<T>`.
- [x] **Відмінно:** Додати підтримку `Comparable`, реалізувати власний ітератор та прямий порядок обходу (Preorder).
